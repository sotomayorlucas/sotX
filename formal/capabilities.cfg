\* TLC config for capabilities.tla
\* Tier 5 follow-up: real model checking. Constants kept tiny because the
\* spec's Rights == SUBSET {5 elements} drives 32x state explosion per cap.

SPECIFICATION Spec

CONSTANTS
    Objects = {o1}
    MaxCaps = 3

INVARIANTS
    TypeInvariant
    NoUnauthorizedAccess
    MonotonicRights
    RevocationComplete

CONSTRAINT AccessBound

CHECK_DEADLOCK FALSE

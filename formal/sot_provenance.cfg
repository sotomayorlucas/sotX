\* TLC config for sot_provenance.tla

SPECIFICATION Spec

CONSTANTS
    RingCap = 3
    Domains = {D1}
    SoObjects = {S1}
    Operations = {"read"}

INVARIANTS
    TypeInvariant
    Completeness
    Ordering

\* Bound the BFS so the unbounded epoch / nextTxId counters don't
\* explode. 6 invocations is enough to exercise drop / drain edges.
CONSTRAINT InvocBound

CHECK_DEADLOCK FALSE

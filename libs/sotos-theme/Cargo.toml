[package]
name = "sotos-theme"
version = "0.1.0"
edition = "2021"
description = "Canonical Tokyo Night palette, geometry, and ANSI helpers for sotOS"

# Zero external dependencies — pure const data.
[dependencies]

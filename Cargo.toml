[workspace]
resolver = "2"
members = [
    "kernel",
    "libs/sotos-common",
]
exclude = [
    "services/init",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = 2

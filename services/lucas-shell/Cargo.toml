[package]
name = "sotos-lucas-shell"
version = "0.1.0"
edition = "2021"

[dependencies]
sotos-lua = { path = "../../libs/sotos-lua" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
